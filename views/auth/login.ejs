<!DOCTYPE html>
<html lang="en" data-theme="<%= theme %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Household Task Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="auth-container">
        <form action="/auth/toggle-theme" method="POST" class="theme-toggle-auth">
            <button type="submit" title="Toggle theme">
                <%= theme === 'dark' ? '☀️' : '🌙' %>
            </button>
        </form>
        
        <div class="auth-card">
            <h2 class="auth-title">Welcome Back</h2>
            
            <% if (error) { %>
                <div class="alert alert-error">
                    <%= error %>
                </div>
            <% } %>
            
            <form action="/auth/login" method="POST">
                <div class="form-group">
                    <label for="email">Email Address:</label>
                    <input type="email" id="email" name="email" class="form-control" 
                           placeholder="Enter your email" required>
                </div>
                
                <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" class="form-control" 
                           placeholder="Enter your password" required>
                </div>
                
                <button type="submit" class="btn btn-primary" style="width: 100%; margin-bottom: 1.5rem;">
                    Sign In
                </button>
            </form>
            
            <div style="text-align: center;">
                <p>Don't have an account? <a href="/auth/register" class="auth-link">Create one here</a></p>
                
                <div class="test-credentials">
                    <strong>Test Account:</strong><br>
                    Email: admin@example.com<br>
                    Password: password
                </div>
            </div>
        </div>
    </div>
</body>
</html>
